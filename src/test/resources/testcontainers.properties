# Testcontainers configuration for CI/CD environments
# Disable Ryuk container (resource cleanup) - helps in CI environments
ryuk.disabled=true

# Docker host configuration (optional, auto-detected in most cases)
# docker.host=unix:///var/run/docker.sock
